# No OpenCode API key or preselected model is configured by this image.
# This file may stay empty for public training use.
# Add local provider environment variables here only when a local
# OpenCode configuration explicitly needs them. Do not commit real secrets.
