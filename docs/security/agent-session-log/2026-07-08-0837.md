# Agent Session Log 2026-07-08 08:37

- Completed mount configuration: added `SECURE_CASE_TRACKER_PROJECTS_DIR`
  and `SECURE_SERVICE_HARVESTER_PROJECTS_DIR` to `.env.example`,
  `compose.yml`, and the repository guidance for the new
  `/secure-case-tracker-projects` and `/secure-service-harvester-projects`
  container mount points.
- Completed local configuration: updated the ignored local `.env` with the
  corresponding host directories without committing or printing secret values.
- Completed Codex configuration update: added the new mount points to
  `codex/config.toml` for the next image build. The currently running image
  still contains its previously baked Codex config until the image is rebuilt.
- Completed documentation update: updated user-facing, agent-guidance, learner,
  and security documentation so the new mounts are discoverable and stay within
  the documented sandbox boundary.
- Completed validation: `podman-compose config` passed.
- Completed home-baseline override validation:
  `podman-compose -f compose.yml -f compose.home-baseline.yml config` passed.
- Completed whitespace validation: `git diff --check` passed.
- Completed runtime refresh without rebuild:
  `podman compose -f compose.yml -f compose.home-baseline.yml up -d
  --force-recreate --no-build` passed.
- Completed runtime mount verification: the recreated `ade` container exposes
  both `/secure-case-tracker-projects` and
  `/secure-service-harvester-projects` from the intended host directories.
- Escalated: no image build, public-release status, formal approval, provider
  approval, legal approval, SBOM, VEX, SLSA, or AI-SBOM status was claimed.
