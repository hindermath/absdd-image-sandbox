# Agent Session Log - 2026-08-19 22:57 CEST

## Umfang / Scope

- Den mit `origin/main` synchronen Repository-Stand nach den letzten
  Governance- und Wartungspaket-Aktualisierungen validiert.
- Das Sandbox-Image mit `podman compose build --pull` neu gebaut und den
  Compose-Container ohne Loeschen persistenter Volumes neu erstellt.
- Einen im neuen Governance-Integritaetstest gefundenen PowerShell-
  Architekturfehler im Image behoben und einen dauerhaften Regressionstest
  ergaenzt. Keine offene Compliance-Plan-ID wurde als abgeschlossen
  beansprucht.

*Validated the synchronized repository state, rebuilt the sandbox image with
pull enabled, recreated the container without deleting persistent volumes,
and fixed a PowerShell architecture issue exposed by the new governance test.
No open compliance plan item was claimed as completed.*

## Pruefungen / Verification

- Repository: `main` und `origin/main` standen vor dem Build auf
  `9996775484f8d0496969afa82f1c0d340197828b` bei Divergenz `0 0`.
  Lock-, Renovate- und Compose-Pruefungen mit und ohne Home-Baseline-Override
  bestanden.
- Qualitaet und Sicherheit: Pre-Commit einschliesslich Gitleaks,
  PSScriptAnalyzer fuer 70 Dateien, 60 Python-Tests mit drei vorgesehenen
  Skips, zehn Dokumentationsfixtures, Shellcheck und `git diff --check`
  bestanden.
- Fehlerbehebung: Das ARM64-Basisimage enthielt im von PowerShell bevorzugten
  Toolpfad einen x86-64-Apphost mit unzureichendem Ausfuehrungsmodus fuer
  `adedev`. Das Dockerfile verlinkt diesen Eintrag nun auf den vorhandenen
  nativen Launcher. Der Smoke-Test prueft eine verschachtelte `pwsh`-
  Ausfuehrung als Nicht-Root-Nutzer.
- Image: Der Vollbuild erzeugte
  `5bec1910211e61f60d140907a75689f9f6e31c2ec5baceaac7ff10b99d846eaf`.
  Der laufende Container verwendet exakt dieses Image und sieht im
  Repository-Mount den Commit `9996775484f8d0496969afa82f1c0d340197828b`.
- Toolchains: Echte Minimal-Builds fuer .NET 10, Java 21, Go 1.26.3, Rust
  1.95.0, Python 3.12.3 und Swift 6.3.3 bestanden. PowerShell 7.6.1
  einschliesslich verschachteltem Prozess, Node.js/npm, SourceKit-LSP, alle
  sechs Agenten-CLIs, Syft und Spec Kit wurden geprueft.
- Governance und Runtime: Der neue Test fuer Delivery-Set, semantisches
  Phase-Result, Lifecycle-Evidenz, historischen Modus und Cross-Shell-Paritaet
  bestand. Beide Agenten-Dispatcher sowie Home-Runtime-Dry-run, Check-only,
  Apply und idempotenter zweiter Apply bestanden schreib- und konfliktgerecht.
- Persistenz: Neutrale Marker blieben in allen sechs geprueften Agenten- und
  Build-Volumes ueber einen Neustart erhalten und wurden danach entfernt.
- SBOM: `sboms/2026-08-19-localhost-absdd-image-sandbox_ade-latest.cdx.json`,
  CycloneDX 1.7, Syft 1.46.0, 23.967 Komponenten und 11.540.283 Byte. Das
  Buildartefakt bleibt ungetrackt.

## Grenzen / Limits

- Die praktische Abnahme deckt macOS mit Apple-Silicon als Host und den
  Linux-arm64-Container ab. Native Windows-/WSL2- und native Linux-Hosttests
  waren nicht verfuegbar.
- Formale Sandbox-Freigabe, externe Register, Secret-Rotation und
  Plattformregeln bleiben menschliche Aufgaben und wurden nicht als erledigt
  dargestellt.

*The practical acceptance covers a macOS Apple-Silicon host and the Linux
arm64 container. Native Windows/WSL2 and native Linux host tests were not
available. Formal approval and other human-only governance tasks remain open.*
