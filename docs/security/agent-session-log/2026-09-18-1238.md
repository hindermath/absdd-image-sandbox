# Agentensitzung / Agent session: Statistik-Zwischenmessung

## Auftrag und Bindung / Authority and binding

Der Owner hat ausschliesslich den ersten Fuenf-Dateien-Patch
`3377017d14467a6fd84048073babf9280d669a3b91789913a8f929b1a7c7b49f`
gesichtet und dessen lokalen Commit sowie die anschliessende reale Messung
freigegeben. Vor dem Commit wurden Arbeitsdateien und Staging-Diff neu
berechnet: beide exakt derselbe SHA-256. Keine weiteren Dateien im Index.

Commit: `9f07e6795cb05ff7d3866972638349c8aded5b44`, auf
`codex/project-statistics-sandbox-field-test`; exakt fuenf Dateien.
Der Benutzername und die Mailadresse wurden aus der vorhandenen
Host-Repositorykonfiguration nur fuer diesen Git-Prozess uebernommen.
Keine persistente Identitaets-, Trust- oder Providerkonfiguration geaendert.
Kein Push, PR, Merge oder weiterer Commit autorisiert oder ausgefuehrt.

The owner reviewed exactly the named five-file patch and authorized one local
commit followed by real measurement. Reconstructed worktree and staged diffs
matched that hash. Only the five reviewed files were committed. Identity was
borrowed from existing host repository configuration for this process only.
No push, PR, merge or follow-up result commit occurred.

## Vorpruefung und Betrieb / Preflight and operation

Die Podman-Startsitzung blieb waehrend der Arbeiten offen. Damit waren VM und
API verfuegbar; keine VM-Reparatur oder Konfigurationsaenderung.
Das vorhandene isolierte Pilotimage wurde per `up -d --no-build` genutzt:
`a33180f5cb18158d894792613479d5864e7a28c64d9308b6fbf7f724a36f4842`.
Nicht-root-Benutzer `adedev`, UID/GID 1655, Linux/aarch64, PowerShell 7.6.4.
Nur genehmigte Pilotmounts; Level-0-Referenz read-only. Urspruenglicher
Checkout bleibt auf `codex/record-sandbox-shutdown`,
`2766887151cf87d1821af3a541fa7b12aa494f74`, unveraendert.

Pre-Commit war im frischen Container nicht installiert und nicht gecacht.
Der laut AGENTS vorgeschriebene `uvx pre-commit`-Prueflauf bezog deshalb
Testwerkzeuge und unveraendert gepinnte Hook-Abhaengigkeiten in einen
isolierten temporaeren Containercache. Kein `uv tool install`, apt, brew,
Image-Neubau oder globale Installation. Version: pre-commit 4.6.2;
Gitleaks-Hook v8.30.1. Alle fuenf `--all-files`-Hooks bestanden.
Zusaetzlich der Secret-Scan fuer genau die fuenf neuen Dateien bestanden.
Worktree- und Staged-Delivery-Set-Validator bestanden; keine fremden Pfade.
Die Vorschau der Compose-Konfiguration bestand. Die 14-Preset-Matrix wurde
unter Bash erneut exakt bestaetigt; Paketdateien blieben unveraendert.

The held-open startup session kept the VM and API available. The original
image and isolated mounts were used without rebuilding or modifying the
original checkout. Required pre-commit tools ran from a temporary container
cache, preserving pinned hooks and avoiding global installation. All full
repository hooks, exact-file secret checks and delivery-set gates passed.

Ergaenzender statischer Koordinator-Review: PSScriptAnalyzer 1.25.0 auf macOS,
zwei Workflow-Inlinebloecke, keine Warning/Error, Exit 0; Workflow-SHA-256
`b9322f9dc61748c27d09a502e0666cf57b7019c60eef4aa034dbb5d63471a89f`.
Dies ist kein Linux-PSSA- oder Windows-Lauf.

The coordinator's macOS static analysis passed for both workflow blocks.
This is supplementary code review, not native Linux or Windows execution.

## Messung und Pruefungen / Measurement and tests

Nach dem genehmigten Commit war der Pilotarbeitsbaum sauber. Bash-Vorschau
und Update mit expliziter Revision `9f07e6795cb05ff7d3866972638349c8aded5b44`
und UTC-Stichtag `2026-09-18` bestanden mit Exit 0.
Ergebnis: 1306 Textdateien, 233454 Textzeilen, 70 UTC-Aktivtage.
Referenz-Modellrechnungen bleiben ausgeschaltet.

- Status im echten Arbeitsbaum unter Bash und PowerShell: Exit 0,
  `CURRENT`, `reproducible=true`, `current=true`, `changed=false`.
  Fuenf Datei-Hashes und der Git-Zustand waren vor/nachher gleich.
- Installierte deterministische Suite erneut bestanden: 67 Unix-Assertions,
  Exit 0, PowerShell 7.6.4. Negative Voraussetzungen werden mit 2,
  Daten-/Reportdrift mit 1 geprueft.
- Zusaetzliche vollstaendige isolierte Git-Kopie: ein synthetischer
  Fixture-Commit nur fuer Messausgaben; kein weiterer Pilotbranch-Commit.
  Zwei weitere Update-Laeufe unter Bash und PowerShell veraenderten nichts
  und behielten die echte Messrevision bei.
- Acht Statusfaelle fuer LF/CRLF mit/ohne BOM unter beiden Shells bestanden,
  einschliesslich unveraenderter Hashes/Git-Zustaende.
- Manipulierter Report wurde mit Exit 1 abgewiesen. Sechs ungefilterte
  Raw-Blob-Faelle bestanden; vier Zeilenendungs-/BOM-Varianten hatten
  verschiedene Git-Objekte, leere und reine BOM-Datei jeweils null Zeilen.
- Bestehenden Profil-2-Ledger in einer getrennten, sauberen Git-Kopie von
  `9f07e67` nach Vorschau gerendert. Beide CheckOnly-Varianten `CURRENT`,
  Exit 0. Nur den erzeugten Ledger zurueckuebernommen und diesen
  Zwischenstand im Fortschreibungsprotokoll erfasst. Konfiguration bleibt
  unveraendert: Europe/Berlin, 73 Aktivtage, dieselben 233454 Textzeilen.
  Unterschiedliche Zeitzonen erklaeren die unterschiedliche Tageszahl.
- Native GitHub-CI und Windows fuer diesen Sandbox-Feldtest noch nicht
  ausgefuehrt. Keine menschliche Feldtest- oder Release-Abnahme behauptet.

The real measurement follows the reviewed clean commit and explicit cutoff.
Both status entrypoints were reproducible, current and read-only. Isolated
fixtures proved repeat updates, eight encoding cases, report-tamper rejection
and six raw blob cases; their synthetic commit is not a delivery commit.
The existing ledger was generated from a separate clean clone with the same
source revision. Its Berlin time zone yields 73 rather than 70 active days.
Native CI, Windows evidence and human field acceptance remain open.

Detailnachweis: [field-evidence.json](../../project-statistics-pilot/field-evidence.json).
Die Read-only-Aussage bindet gepruefte Hashes und Git-Zustaende; kein
vollstaendiger Betriebssystem-I/O-Trace. Temporare Testdateien werden
bewusst geschrieben und sind vom Read-only-Payload getrennt.
`sourceHashes` bindet den Pruefzeitpunkt vor dem separaten Legacy-Rendering
und den Dokumentationsnachtraegen, nicht die finalen Review-Dateibytes.
Der vollstaendige neue Patch erhaelt deshalb einen eigenen SHA-256.

The linked record binds hashes, revisions, exact outcomes and proof limits.
Fixture writes are intentional; this is not a complete operating-system I/O
trace.
`sourceHashes` describes checked bytes before the separate legacy render and
documentation follow-ups; the final review patch has its own hash.

## Neue Sichtungsgrenze / New review boundary

Messausgaben, Nachweise, README-Fortschreibung, Ledger und dieses Protokoll
bleiben uncommittet bis zur erneuten menschlichen Sichtung.
Die Messung ist reproduzierbare **Zwischenevidence auf 9f07e67**, kein
abschliessender branchaktueller Liefernachweis: dieses neue Sitzungsprotokoll
liegt ausserhalb des ausgeschlossenen Pilotkontexts und wird nach seinem
gesichteten Commit Teil der Messquelle. Danach sind beide Statistiken erneut
auf sauberem Arbeitsbaum zu aktualisieren und der neue Diff zu sichten.

All new outputs and documentation remain uncommitted pending fresh review.
The measurement is reproducible intermediate evidence, not final delivery
freshness. This new session log becomes measured source after its reviewed
commit; both statistics must then be refreshed from a clean tree and the new
diff reviewed. No dirty-tree bypass, stash or extra commit was used.

Dokumentationsauswirkung: `UpdateRequired`; Owner/Reviewer Thorsten
Hindermann, Lernende/Maintainer/Reviewer als Zielgruppen; Leserpfad:
Pilot-README -> Messbericht -> JSON-Evidence -> Sitzungsprotokoll.
ActiveSemantic, DE zuerst/EN danach, Projektdokumentation ohne Home-Sync.
Wiedervorlage nach jedem Inhaltscommit beziehungsweise Paket-/Profilwechsel.
Die unveraenderten vorhandenen Quellen und Renderer erzeugen die Ausgaben.
NIST SSDF/CWE und Quellenbindung gelten; kein Web/API-, Image- oder
Modellruntime-Delta, keine neue C5-/Konformitaets-/Zertifizierungsentscheidung.

UpdateRequired; the maintainer owns bilingual project documentation and
derived evidence, with no Home sync. Reevaluate after content commits or
package/profile changes. Existing renderers and configurations stay unchanged.
No new application, image, model runtime or compliance decision is introduced.

## Plan-IDs und sicherer Abschluss / Plan IDs and safe closeout

P2-1 `partial`: lokale Zwischenmessung bestanden, Gesamtabnahme offen.
P1-3 `done` fuer den exakt geprueften ersten Commit.
P1-1 `done` fuer den heutigen Audit-Export: Exit 0, fuenf reine
Metadatenzeilen in `/audit/2026-09-18.jsonl`.
P1-4 und P2-2 bleiben menschliche beziehungsweise Plattformentscheidungen;
keine formelle Freigabe oder Bypass. Andere Compliance-Aufgaben ausserhalb
des Auftrags. Die nach diesem Log folgende Sequenz ist erneut Audit-Export
und Compose-Down ohne `-v`; der Koordinator bestaetigt anschliessend
Volume-/Image-Erhalt und VM-Stopp im Chat. Kein Neustart nur fuer Lognachtrag.

P2-1 remains partial; field acceptance is open. Secret checks passed for the
authorized commit and the metadata-only audit export succeeded. Formal
approval and platform controls remain unchanged. Following this log, audited
Compose shutdown preserves volumes, and the coordinator reports final VM,
image and volume checks without restarting solely to update this document.
