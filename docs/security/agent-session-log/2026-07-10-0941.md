# Agent Session Log - 2026-07-10 09:41

- Scope: assessed the feasibility question "bake VS Code Server into the image so
  a learner can run `code <file>` in bash and have host VS Code connect into the
  container", then documented the already-supported workflow for learners.
- Finding (reported, no code change to image): the described mechanism does not
  work. `code <file>` from a cold container shell cannot launch or connect host
  VS Code; the `code` command is a shim that talks to an already-attached host
  VS Code over an IPC socket. Baking a server into the image is unnecessary and
  intentionally avoided (VS Code installs its matching remote server itself on
  attach; documented in sandbox-isolation.md / sandbox-freigabe.md /
  public-readiness-evidence.md, no IDE service, no extra port). The desired
  outcome is already supported via `.devcontainer/devcontainer.json` and the
  README section "VS Code von aussen verbinden".
- Completed: added `docs/fuer-lernende/vscode-dev-containers.md` — beginner
  guide to the Dev Containers attach workflow (prerequisites incl. the Podman
  setting `dev.containers.dockerPath: podman`, step-by-step attach, the truth
  and limits of `code <file>`, relation to the Container-First Gate, and
  troubleshooting).
- Completed: linked the new document from `docs/fuer-lernende/README.md`
  (learning-path box as optional step 5, and the "where do I find what" table).
- Completed: extended `docs/fuer-lernende/GLOSSAR.md` with Dev Container, LSP,
  and IPC-Shim (`code`).
- Facts reused, not invented: README "VS Code von aussen verbinden" (authoritative
  workflow, container/service names, no-baked-server statement),
  `.devcontainer/devcontainer.json`, `docs/security/sandbox-isolation.md`, and
  the installed LSP servers from `Dockerfile` (gopls, rust-analyzer,
  sourcekit-lsp).
- Completed validation: `git diff --check` passed; DE-first confirmed; all code
  fences language-tagged (WCAG 4.1.1); heading hierarchy has no gaps; new doc is
  consistent with the README section (same names, same stop path, same
  no-server-in-image statement).
- Not changed: no Dockerfile, compose.yml, or port changes; no code-server /
  openvscode-server / `code tunnel`; no baked VS Code server. The Podman setting
  hint was scoped to the learner docs, not added to the main README this session.
- Not claimed / out of scope: no human-only items claimed complete (formal
  sandbox approval, branch protection, external registers).
