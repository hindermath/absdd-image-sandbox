# Agentensitzung / Agent session: Statistik-Feldtest, erste Review-Etappe

## Auftrag und Ergebnis / Authority and result

Am 2026-09-18 parallel zum getrennten TinyCalc-Piloten begonnen. Sandbox-Arbeit
ausschliesslich in der isolierten Pilot-Arbeitskopie und als `adedev` im
bestehenden Pilotimage. Branch: `codex/project-statistics-sandbox-field-test`.
HEAD bleibt `457cc3b32872cadebefab613423e5542dc34760b`.
Keine Commits, kein Push, kein PR oder Merge; Sichtung vor Commit/Push offen.

Started the independently scoped sandbox pilot in parallel with TinyCalc.
All tracked edits and tests ran as non-root adedev inside the pilot container.
The branch remains at the stated baseline. No commit, push, PR or merge;
human review is pending before commit/push.

## Ausgefuehrte Pruefungen / Executed checks

- Isolierte Arbeitskopie vor Beginn sauber, `main` zu `origin/main` 0/0;
  vollstaendige Git-Historie, kein Shallow-/Promisor-Repository.
- Vorhandenes Pilotimage:
  `a33180f5cb18158d894792613479d5864e7a28c64d9308b6fbf7f724a36f4842`.
  Spec Kit 0.12.8, PowerShell 7.6.4, Linux/aarch64, UID/GID 1655.
- Gestoppte Podman-VM gestartet; der vorher entfernte Pilotcontainer wurde
  unter demselben isolierten Compose-Projekt mit `up -d --no-build`
  wiedererstellt. Exaktes Image und Mounts vor Arbeitsbeginn geprueft.
  Keine Fremd-Checkout-Mounts; Level-0-Referenz nur read-only.
- `podman-compose config`: Exit 0; keine Secrets eingefuegt.
- 14-Preset-CheckOnly mit expliziter Matrix unter Bash und PowerShell:
  jeweils Exit 0. Alle 26 Paketdateien stimmen mit dem Installationsreceipt
  ueberein; zentraler Koordinator verglich zusaetzlich das frisch
  heruntergeladene, SHA-256-gepruefte Tag-ZIP.
- `specify preset resolve project-statistics-contract`: Exit 0.
- Installierte `tests/test-project-statistics.ps1`: Exit 0,
  `PASS: 67 assertions; platform=Unix; PowerShell=7.6.4`.
- Init-Vorschau: `DRY_RUN`, `changed=false`, Exit 0.
  Init: `INITIALIZED`, `changed=true`, Exit 0.
- Vorhandene integrierte Codex-`apply_patch`-Implementierung fuer manuelle
  Container-Edits genutzt; keine Tools installiert oder Sicherheitsoptionen
  erweitert.
- Reale Messung/Update, deren Paritaet und native CI: noch nicht ausgefuehrt.
  Die CI-Datei ist vorbereitet und erwartet den spaeter erzeugten Snapshot.
- YAML-Parsing und JSON-Schema der Konfiguration: bestanden. Beide
  mehrzeiligen PowerShell-CI-Bloecke parsen fehlerfrei. Die erste lokale
  Extraktionspruefung war wegen eines zu breiten Regex ungueltig; nach
  Korrektur wurde die gesamte Syntaxpruefung erneut ausgefuehrt. Kein
  Workflow-Defekt daraus abgeleitet. PSScriptAnalyzer ist im Pilotimage
  nicht installiert; keine Nachinstallation, dieses Gate bleibt offen.
- Audit-Export: Exit 0, fuenf Metadatenzeilen in `/audit/2026-09-18.jsonl`;
  keine Prompt- oder Antwortinhalte.

The initial Git, image, tool, matrix and 26-file receipt checks passed.
The installed isolated fixture suite passed 67 Unix assertions; init preview
and init passed. Actual project measurement and its native CI remain pending.
The workflow cannot establish acceptance until the reviewed configuration is
committed and a real snapshot exists. Only metadata was exported for audit.

## Plan-IDs, Sicherheit und Wartegrenze / Plan IDs, security and pause

- P2-1: `partial`; Statistik-Pilot initialisiert, Feldtest noch offen.
- P1-1: `done` fuer den heutigen Audit-Export; Betriebsnachweis des
  anschliessenden geordneten Stopps folgt im Sitzungsabschluss.
- P1-3: umfassender Pre-Commit-/Secret-Scan folgt vor autorisiertem Commit;
  kein Commit in dieser Etappe.
- P1-4: formelle Sandbox-Freigabe bleibt menschlich und unveraendert.
- P2-2: Plattformschutz unveraendert; kein Bypass ausgefuehrt.
- Uebrige Compliance-Aufgaben: uebersprungen, ausserhalb dieses Auftrags.

NIST SSDF und CWE Top 25: Quellen-/Dateipfad- und CI-Sicherheitspruefung.
SLSA: Hash-/Commitbindung ohne Level-Behauptung. ASVS N/A (kein Web/API-Delta);
Image-SBOM-Neubau N/A (unveraendertes Image); AI-SBOM fuer Modellruntime N/A
(nur Entwicklungswerkzeuge); keine neue VEX-Disposition oder
Regulatorik-/C5-/Zertifizierungsentscheidung.

Die Git-Vertrauensausnahme gilt nur pro Prozess fuer `/ade-dev-sandbox`.
Urspruenglicher Checkout weiterhin auf `codex/record-sandbox-shutdown`,
Commit `2766887151cf87d1821af3a541fa7b12aa494f74`; keine Aenderung dort.
Bestehender Statistik-Ledger ist unveraendert; Fortschreibung erst nach dem
gesichteten Inhaltscommit. Kein Home-Sync, Registry-Write oder Image-Neubau.

Keep human review separate from fixture success. The process-local Git trust
exception is limited to the pilot mount; original checkout, existing ledger,
global configuration and image remain unchanged. Next: review the exact
configuration/documentation/workflow patch, authorize its commit, then preview
and run the real measurement on a clean tree. Review generated follow-ups again.
