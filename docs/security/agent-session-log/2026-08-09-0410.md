# Agent Session Log - 2026-08-09 04:10 CEST

## Umfang / Scope

- Home-Baseline `v0.17.0` am Commit
  `7d7da767c3a83eb6ca54c752250122fc048b8318` in das Sandbox-Image gepinnt.
- `~/home-baseline-source` als kanonischen Link und `~/home-baseline-tmp` als
  veralteten Kompatibilitaetslink eingerichtet.
- Expliziten Drei-Modus-Wrapper `sync-home-baseline-runtime` fuer die
  manifestdefinierte Home Runtime installiert und dokumentiert.
- Keine Compliance-Plan-ID abgeschlossen oder als formal freigegeben
  beansprucht; offene menschliche Freigaben aus `COMPLIANCE-PLAN_RL-SE-001.md`
  bleiben unveraendert eskaliert.

*Pinned Home Baseline `v0.17.0`, added the canonical and compatibility links,
and installed the explicit three-mode Home Runtime wrapper. No compliance-plan
item or formal human approval is claimed as completed.*

## Pruefungen / Verification

- Level-0-Ruhe-Gate: zwei identische inaktive Pruefungen im Abstand von 300
  Sekunden; bei wieder aufgetretener Fremdaktivitaet geschlossen und nach zwei
  weiteren identischen inaktiven Pruefungen erneut geoeffnet.
- Level 0: 97 Python-Tests bestanden, 12 erwartete Plattform-Skips;
  PSScriptAnalyzer fuer 163 Dateien bestanden; Bash-/PowerShell-Paritaet,
  Symlink-Escape, Konflikt-, Credential- und Kategorien-Schutz bestanden.
- `podman-compose config` ohne Override und mit
  `HOME_BASELINE_DIR=/Users/thorstenhindermann/home-baseline-source` bestanden.
- `podman compose build --pull` bestanden; finales Image
  `96eeaa04cc78a2fc982d36850729e2a698dd4d20d7fb486afcdc1b1145da4f43`.
- Wrapper ohne Modus, Dry-run, Check-only, Apply, zweiter idempotenter Apply,
  normaler Container-Sync-Block, Neustart, Recreate und erneuter Apply
  bestanden. Fuenf Persistenz-Sentinels blieben erhalten.
- `sourceOnly`, `machineLocal`, `.gitconfig`, Home-Git-Repository und
  Git-Identitaetsaenderungen blieben ausgeschlossen.
- Toolchain-Smoke-Test fuer .NET, Java, Go, Rust, Python, Node.js und Swift
  sowie CLI-Starts fuer OpenCode, Codex, Claude Code, Antigravity, Copilot,
  Syft und Spec Kit bestanden. OpenCode-Providerkatalog war verfuegbar.
- Documentation-Impact-Validator und Fixtures, ShellCheck, `shfmt`,
  PSScriptAnalyzer, Pre-Commit, Gitleaks und `git diff --check` bestanden.
- CycloneDX 1.7 SBOM mit 23.964 Komponenten erfolgreich erzeugt; das
  ignorierte Artefakt liegt unter `sboms/`.

*The final image, all wrapper modes, persistence behavior, language and agent
CLI smoke tests, documentation checks, static analysis, secret scan, and the
CycloneDX SBOM passed.*

## Plattformabdeckung / Platform Coverage

- Praktische Host- und Podman-Lebenszykluspruefung: macOS auf Apple Silicon mit
  Linux-arm64-Podman-Machine.
- Windows/WSL2 und nativer Linux-Host waren in dieser Sitzung nicht verfuegbar;
  die PowerShell-Analyse lief lokal mit PowerShell 7, aber ohne dortigen
  Container-Lebenszyklus.

*Practical lifecycle coverage was macOS/Apple Silicon with a Linux-arm64
Podman machine. Windows/WSL2 and a native Linux host were unavailable.*
