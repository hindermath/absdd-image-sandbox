# Agent Session Log 2026-07-10 15:25 CEST

- Scope: approved learner-onboarding plan, four required agent CLIs, Syft,
  persistent agent state, audit boundaries, documentation CI, and Podman-only
  learner guidance.
- Completed: installed and pinned Codex, Claude Code, Gemini CLI, GitHub Copilot
  CLI, and Syft in the image; added separate state volumes and Renovate/static
  parity checks; documented personal `home-baseline` fork onboarding and the
  first controlled agent run.
- Completed validation: Dockerfile/Compose static checks, Bash and PowerShell
  parsing, Lychee offline file/anchor check, full Podman image build on arm64,
  all six language smoke tests, four agent version checks, Spec Kit, Syft,
  non-root hardening, audit export, volume persistence, and volume removal.
- Supply-chain evidence: Swift signature, Syft checksum, uv checksum, and
  rustup checksum validated during the build. CycloneDX 1.7 SBOM generated with
  the image-integrated Syft and confirmed to contain all four agent packages.
- Corrected during validation: the host Syft image path exhausted temporary
  disk space while exporting a second image copy. The SBOM wrappers now use the
  Syft binary pinned in the final image and avoid that duplicate host export.
- Audit boundary: exported five tool-version records only; no prompt, response,
  token, credential, or secret content was present. Gemini and Copilot session
  collection remains `Open` pending stable, narrowly allow-listable formats.
- Human-only items unchanged: account/provider/model/legal approvals and formal
  sandbox approval remain `Open`; this session did not claim or grant them.
