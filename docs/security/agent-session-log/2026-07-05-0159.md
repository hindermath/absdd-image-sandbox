# Agent Session Log 2026-07-05 01:59

- Completed: removed the empty OpenCode provider allowlist from `opencode.jsonc` so the built-in provider picker is available again while no API key or preselected model is shipped in the image.
- Completed: updated README, AGENTS guidance, Copilot instructions, OpenCode env example, public-readiness evidence, and AI tool inventory to distinguish "no preconfigured API key/model" from "provider picker disabled".
- Completed: changed the default image tag in `scripts/build-and-sbom.sh` and `scripts/build-and-sbom.ps1` to `absdd-image-sandbox-ade:latest`, matching the current Compose build output.
- Completed verification: `git diff --check`, `python3 scripts/check-dockerfile-arg-renovate.py`, `podman-compose config`, and `podman compose build --pull`.
- Completed evidence: rebuilt image `absdd-image-sandbox-ade:latest` with image ID `e36767fa1c069ab7e76fb184f8b98535e0e5d1f17788d16d2edecdbb79706ff8`.
- Completed evidence: `opencode debug config` no longer contains `enabled_providers`, and `timeout 5s opencode providers login` shows standard providers including `OpenCode Zen (recommended)` and `OpenAI`.
- Completed evidence: `/etc/codex/requirements.toml` is readable by `adedev`, and `codex` no longer fails with a requirements-file permission error.
- Partial P2-3: SBOM generation was retried with the corrected default tag via `scripts/build-and-sbom.sh --skip-build`; the Syft container run failed with a Podman API EOF, left an empty SBOM file, and destabilized the local Podman machine.
- Completed cleanup: removed the empty `sboms/2026-07-05-absdd-image-sandbox-ade-latest.cdx.json` artifact after the failed SBOM attempt.
- Escalated: a successful SBOM for `absdd-image-sandbox-ade:latest` still requires a stable Podman machine or a host-local Syft installation; this session did not change Podman machine resources automatically.
- Escalated: formal sandbox approval, provider/model approval, release VEX/SLSA publication decisions, platform governance, and cross-platform validation remain human-only/open items.
