# Wartungspaket-Verteilung / Maintenance package distribution

## Deutsch

Owner-Auftrag: die gepruefte Paketkorrektur aus Home-Baseline PR 309 in die
35 kanonischen Flottenziele verteilen und mit MergeAndSync abschliessen.
Dieses Repository erhaelt dieselben vier Paketdateien: zwei Container-Helfer,
Vertragstests und Wartungs-Manpage. Git-Vertrauen bleibt prozessgebunden und
auf exakt freigegebene Repository-Pfade beschraenkt. Kein globaler Wildcard-
Eintrag, keine Aenderung an Image, Volumes, Provider oder Zugangsdaten.

Compliance-Plan: keine neue P0/P1/P2/P3-Aufgabe abgeschlossen. Die bestehenden
Human-only-Entscheidungen bleiben beim Owner; dessen befristete Freigabe wird
nicht ersetzt. P1-1: Sitzungsnachweis ergaenzt. Uebrige Aufgaben unveraendert,
weil der Auftrag ausschliesslich die freigegebene Paketverteilung betrifft.

Pruefung: `podman-compose config` bestanden; Paketvergleich, lokale Tests,
Secret-Hooks und Statistikpruefung sind vor der Veroeffentlichung verbindlich.
Zentrale plattformuebergreifende Evidence: Home-Baseline PR 309 (18 gruene
CI-Pruefungen); laufendes Image enthaelt bereits den geprueften Paketstand.
Empfehlung: nach dem Flottenabschluss den vollstaendigen Wartungscheck lesen.
Documentation Impact: UpdateRequired, Manpage und dieser Sitzungsnachweis.

## English

The owner authorized distribution of Home-Baseline PR 309 to the 35 canonical
fleet repositories, followed by MergeAndSync. This repository receives the
same four package files. Git trust remains process-scoped and restricted to
approved repository paths. No image, volume, provider or credential changes.
No new compliance-plan task or human-only approval is claimed complete;
this entry supplements P1-1 session evidence. Compose configuration passed.
Package comparison, local tests, real secret hooks and statistics verification
are required before publication. Central cross-platform proof is PR 309 with
18 successful CI checks. Read the final fleet maintenance check after delivery.
