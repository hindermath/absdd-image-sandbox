# Agent Session Log - 2026-07-08 21:56

- Completed documentation update: README now documents platform-aware Podman
  endpoint selection for macOS, Windows, and Linux without committing a local
  host socket path.
- Completed agent-guidance parity update: AGENTS.md, CLAUDE.md, GEMINI.md,
  and .github/copilot-instructions.md now describe how agents should treat
  Podman machine endpoints, `CONTAINER_HOST`, and `DOCKER_HOST`.
- Clarified that `DOCKER_HOST` means Docker-compatible API access to
  Podman-backed tooling and does not reintroduce Docker as the target runtime.
- Completed validation: `git diff --check` passed.
- Completed validation: `podman-compose config` passed.
- Completed validation: searched the updated guidance files for fixed
  `127.0.0.1:60363` and `/var/folders/...podman-machine-default-api.sock`
  references; no committed fixed local endpoint was found.
- Escalated: no Windows or Linux host validation was available in this
  session, so those instructions are documented as platform guidance only.
