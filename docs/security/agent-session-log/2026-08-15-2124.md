# Agent Session Log - 2026-08-15 21:24 CEST

## Umfang / Scope

- Den aktuellen Stand von `main` als
  `localhost/absdd-image-sandbox_ade:latest` mit `podman compose build --pull`
  neu gebaut und den Compose-Container ohne Loeschen persistenter Volumes neu
  erstellt.
- Das Image, die sechs MSL-Sprachtoolchains, PowerShell, Agenten-CLIs,
  Dispatcher, Home-Baseline-Runtime, Mounts und Volume-Persistenz praktisch
  geprueft.
- Eine neue CycloneDX-SBOM aus dem finalen Image erzeugt. Keine offene
  Compliance-Plan-ID wurde als abgeschlossen beansprucht.

*Rebuilt the current `main` image with pull enabled, recreated the Compose
container without deleting persistent volumes, exercised the toolchains and
runtime controls, and generated a fresh CycloneDX SBOM. No open compliance
plan item was claimed as completed.*

## Pruefungen / Verification

- Repository: `main` und `origin/main` standen vor dem Build auf
  `958923d1ea150bfdde28a8e99f3ea57e4da093c5` bei Divergenz `0 0`.
  Renovate-Metadaten, Home-Baseline-Lock, Compose mit und ohne persoenlichen
  Override, Pre-Commit einschliesslich Gitleaks, PSScriptAnalyzer, 60
  Python-Tests, zehn Dokumentationsfixtures und `git diff --check` bestanden.
- Image-Build: `podman compose build --pull` bestand auf macOS mit
  Apple-Silicon und erzeugte Image-ID
  `9ec737bf09e20740b1c8b9442c449b829ab679321a6e4ffd79d78502a4819fd8`.
  Das Image bettet Home Baseline `v0.18.0` am Commit
  `a0fc446cd04db2e52102a3b2be31c24e8ee6a022` schreibgeschuetzt ein.
- Toolchains: echte Minimal-Builds fuer .NET 10, Java 21, Go 1.26.3, Rust
  1.95.0, Python 3.12.3 und Swift 6.3.3 bestanden. PowerShell 7.6.1,
  Node.js/npm, SourceKit-LSP, OpenCode, Codex, Claude Code, Gemini CLI,
  Antigravity CLI, Copilot CLI, Syft und Spec Kit wurden geprueft.
- Agenten: Codex konnte die Konfiguration unter `/etc/codex` mit Modus `0644`
  lesen. Bash- und PowerShell-Dispatcher erzeugten fuer alle sechs Agenten
  identische redigierte Dry-run-Befehle. OpenCode aktualisierte den
  Modellcache und stellte ohne persoenliche Anmeldung den eingebauten
  `opencode`-Provider bereit.
- Home Runtime: Ein Aufruf ohne Modus schrieb nichts und endete mit Exit 2.
  Dry-run blieb schreibfrei; Check-only erkannte den erwarteten Drift. Im
  Wegwerfcontainer bestanden Apply, Check-only und ein zweiter idempotenter
  Apply. `sourceOnly`,
  `machineLocal`, Git-Konfiguration und Credential-Sentinels blieben
  unangetastet.
- Persistenz: Die bestehenden benannten Volumes wurden beim Recreate erneut
  eingebunden. Neutrale Marker blieben in allen sechs geprueften Volumes ueber
  einen Neustart erhalten und wurden danach entfernt.
- SBOM: `sboms/2026-08-15-localhost-absdd-image-sandbox_ade-latest.cdx.json`,
  CycloneDX 1.7, Syft 1.46.0, 23.967 Komponenten und 11.540.283 Byte. Das
  Buildartefakt bleibt ungetrackt.

## Teilweise abgeschlossen und eskaliert / Partial and Escalated

- Beim ersten .NET-Minimal-Build trat einmalig `Illegal instruction` mit Exit
  132 auf. Direkte SDK-Aufrufe, drei frische Restores und die vollstaendige
  wiederholte Smoke-Suite liefen anschliessend ohne Workaround erfolgreich;
  der Fehler war nicht reproduzierbar und fuehrte deshalb zu keiner breiten
  Deaktivierung von Hardware-Intrinsics.
- Der praktische Image-Test deckt den macOS-Host mit Apple-Silicon und den
  Linux-arm64-Container ab. Native Windows-/WSL2- und native Linux-Hosttests
  waren nicht verfuegbar. Formale Sandbox-Freigabe, externe Register,
  Secret-Rotation und Plattformregeln bleiben menschliche Aufgaben.

*The first .NET minimal build hit one non-reproducible illegal-instruction
exit. Direct SDK checks, three fresh restores, and the repeated full smoke
suite then passed without a workaround, so hardware intrinsics were not
disabled globally. Native Windows/WSL2 and native Linux host checks were not
available. Formal approval and other human-only governance tasks remain open.*
