# Spec-Kit-Upgrade / Spec Kit upgrade

## Auftrag / Scope

Thorsten hat die prozessbezogene Git-Vertrauensausnahme nur fuer den
geprueften Pilotmount genehmigt und Spec Kit 0.12.8 fuer die Sandbox beauftragt.
Allgemeine Wartung bleibt ausgeschlossen. Arbeitsbranch:
`codex/sandbox-spec-kit-0128`; urspruenglicher Shutdown-Branch unveraendert.

Thorsten authorized process-scoped Git trust for the verified pilot mount and
the targeted sandbox CLI upgrade. No general maintenance; original branch
preserved. The previous preflight blockers are resolved for the test image.

## Ergebnis / Result

Spec Kit auf Release-Commit gepinnt, notwendige Patch-Kompatibilitaet ergaenzt,
drei Regressionstests lokal und in CI eingebunden. Image-Build, Toolchain-Smokes,
Init-/Preset-Lifecycle, Patch-Idempotenz, Agent-Paritaet und SBOM erfolgreich.
Details und Hashes: [Upgrade-Nachweis](../../maintenance/spec-kit-0128-upgrade.md).
Temporäre Initialisierungs-Testdateien entfernt; keine Nutzerdaten geloescht.
Keine Sandbox-Preset-Installation aktualisiert, kein Feature gestartet.

Pinned release, compatible copy patch and regression tests prepared. Build,
runtime and lifecycle checks plus SBOM succeeded; see the linked exact evidence.
Only temporary test fixtures were removed. No preset rollout or feature run.

## Status und Eskalation / Status and escalation

Upgrade: partial, technisch lokal geprueft; menschliche Diff-Pruefung vor
Commit/Push offen. Kein Commit, Push oder Merge in diesem Repository.
Zusaetzlicher Vollscan-Treffer am unveraenderten oeffentlichen Swift-Fingerprint
zur Sichtung gemeldet, keine Unterdrueckung. Standardimage unveraendert.
Keine Compliance-Plan-Aufgabe neu als abgeschlossen markiert. P1-4 bleibt
menschlich; uebrige Compliance-Aufgaben ausserhalb des Upgrade-Auftrags.
Naechster Schritt: konkreten Diff freigeben, danach geregelte Lieferung und
Statistik-Preset-Pilot fortsetzen.

Upgrade partial: locally tested, human pre-commit/push review still open. No
repository commit, push or merge. The unchanged public fingerprint finding is
disclosed without suppression. No formal compliance approval or unrelated work.
Review the exact candidate, deliver it, then resume the statistics preset pilot.
