# Agent Session Log - 2026-07-10 08:01

- Scope: learner-documentation gap assessment for year-1 IT apprentices with no
  container prior knowledge, plus comprehensive beginner onboarding to close the
  gap. Driven by the ISO/IEC 27001 requirement that learners engage with
  containerization from year 1.
- Assessment result: governance/evidence documentation (sandbox profile, mount
  matrix, MSL matrix, agent parity, network decision, templates) already meets
  the year-1 learning-series bar; the beginner-onboarding layer was the gap.
- Completed: added five beginner documents under `docs/fuer-lernende/`:
  `container-grundlagen.md` (container/image/volume/mount basics + ASCII
  diagram), `warum-sandbox.md` (Container-First Gate, four protection goals,
  ISO/IEC 27001 controls A.5.23/A.8.25/A.8.28/A.8.31, common beginner mistakes,
  short preflight), `installation.md` (per-OS Podman install with expected
  output), `erste-schritte.md` (first-day red thread to first success and clean
  audit-compliant stop), `troubleshooting.md` (symptom/cause/solution catalog).
- Completed: reworked `docs/fuer-lernende/README.md` into a hub — added a
  "New here? No prior knowledge?" learning path, condensed the quick start to a
  short version that points to the detailed guides, extended the "where do I
  find what" table, and sharpened the year-1 framing (gate is binding from
  year 1; only practical write use is a justified `N/A`).
- Completed: extended `docs/fuer-lernende/GLOSSAR.md` with beginner terms
  (Image, Registry, Host, Podman-Machine, Bind-Mount, Volume, Bridge-Netz).
- Facts reused from source of truth, not invented: mounts/ports/hardening from
  `compose.yml`; egress and package sources from `docs/security/network-decision.md`;
  MSL versions, mount matrix, and stop command from `docs/fuer-lernende/sandbox-profil.md`.
- Completed validation: `git diff --check` passed (no whitespace errors).
- Completed validation: DE-first check — first heading and body of every new
  file are German.
- Completed validation: every opening code fence is language-tagged
  (WCAG 4.1.1); ASCII diagram uses a `text` fence; heading hierarchy has no
  gaps (only h1/h2).
- Completed validation: `podman-compose config` passed (changes are Markdown
  only).
- Not claimed / out of scope: no human-only items claimed complete (formal
  sandbox approval, API-key rotation, platform branch protection, external
  registers); no `_TODO_` owner fields invented; learning-series study
  companions in `home-baseline-tmp` were referenced, not copied into this repo.
- Escalated: full practical macOS/Windows/Linux start-to-stop walkthrough was
  not executed end-to-end this session; the install/first-day steps are
  documented and config-validated, cross-platform practical acceptance remains
  open.
