# Statistik-Pilot: Vorpruefung / Statistics pilot preflight

## Auftrag und Ergebnis / Scope and result

Project Statistics Governance v0.1.0, Tracking: [#70](https://github.com/hindermath/absdd-image-sandbox/issues/70).
Der vollstaendige Pilot bleibt beauftragt, ist aber noch nicht installiert.
Ein separater Klon auf `codex/project-statistics-pilot` bewahrt den bestehenden
Shutdown-Branch samt dessen lokalem Commit. Ausgangspunkt: `eb97bf9`.

The full pilot remains authorized but is not installed. A separate clone on
the pilot branch preserves the original shutdown branch and local commit.

## Ausgefuehrte Pruefungen / Executed checks

- `podman-compose config`: Exitcode 0.
- `podman compose config`: Exitcode 0.
- Bestehendes Image ohne Neubau gestartet; Containerbenutzer `adedev`.
- Pilotklon ist unter `/ade-dev-sandbox` eingebunden.
- PowerShell im Container: 7.6.4; Spec Kit: 0.8.3.
- Git meldet `detected dubious ownership`; keine Vertrauensausnahme gesetzt.
- Das Preset benoetigt Spec Kit ab 0.12.8; keine Toolchain aktualisiert.

Configuration checks passed; the existing image runs as non-root `adedev`.
Git rejects the mounted clone ownership. No trust exception or toolchain
change was made. The installed Spec Kit is below the preset requirement.

## Eskalation und Grenzen / Escalation and boundaries

Statistik-Pilot: partial. Keine Compliance-Plan-Aufgabe neu abgeschlossen.
P1-4 und menschliche Pruefung vor Commit/Push bleiben menschliche Gates.
Uebrige Compliance-Aufgaben nicht bearbeitet: ausserhalb des Statistikauftrags.
Keine Secrets gelesen oder kopiert, keine Volumes entfernt, keine
Preset-Installation, kein Commit und kein Push im Sandbox-Repository.

Pilot partial; no compliance-plan item newly completed. P1-4 and human review
before commit/push remain human-only. Other compliance work is outside scope.
No secrets copied, volumes removed, preset installed, commit or push performed.

Naechster Schritt: Owner-Entscheidung zur eng begrenzten Git-Vertrauensausnahme
fuer den geprueften Pilotmount und zur isolierten, gepinnten Spec-Kit-Version
fuer diesen Pilot. Anschliessend Installation, Tests und konkreten Diff vor
Commit/Push zur menschlichen Pruefung vorlegen.

Next: owner decision on narrowly scoped Git trust for the verified mount and
an isolated pinned Spec Kit version. Then install, test and present the exact
diff for human review before commit/push.
