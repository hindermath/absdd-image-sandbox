# Agent Session Log - 2026-07-11 00:05 CEST

## Scope

- Embedded `home-baseline` as the default Level-0 reference for the sandbox
  image.
- Optional personal `home-baseline` override via `compose.home-baseline.yml`.
- Runtime validation on local macOS with Podman Machine.
- No repository visibility change, no provider/model configuration, no
  secrets, and no formal sandbox approval.

## Completed

- Added a lock-file based `home-baseline` reference:
  - source: `https://github.com/hindermath/home-baseline.git`
  - tag: `v0.12.0`
  - commit: `6bb34cd24df3d9a460e187b7f145ea5bf36ddfa5`
  - license: `MIT`
- Added an image installer that fetches the locked release, checks out the
  exact commit, removes write bits, and registers `/opt/home-baseline` as a
  Git safe directory.
- Exposed the embedded reference as `/opt/home-baseline` and the learner alias
  `/home/adedev/home-baseline-tmp`.
- Updated the optional override so `HOME_BASELINE_DIR` bind-mounts over
  `/opt/home-baseline`, preserving the same learner alias.
- Synchronized README, learner documentation, security documentation, and all
  maintained agent guidance surfaces with the embedded-reference model.
- Added lock validation to pre-commit and CI-oriented documentation checks.

## Runtime Validation

- Built image with Podman:
  `localhost/absdd-image-sandbox_ade:latest`
  (`dfa21d0f954855d0735a2f9dc7a4cdde59633e5fe779d574f5cda47803ee1463`).
- Default container validation passed:
  - `/home/adedev/home-baseline-tmp` resolves to `/opt/home-baseline`.
  - `/opt/home-baseline` is image content, not a host mount.
  - HEAD matches `6bb34cd24df3d9a460e187b7f145ea5bf36ddfa5`.
  - `upstream` points to the public `home-baseline` GitHub repository.
  - no `origin` remote is configured in the embedded reference.
  - the embedded reference is read-only for `adedev`.
  - `git status` works on the detached reference.
- Optional override validation passed with a temporary clean checkout under
  `/private/tmp/absdd-home-baseline-override.3OUZ5Q/home-baseline`:
  - same learner alias and locked commit.
  - `/opt/home-baseline` is mounted from the provided host checkout.
  - write test in the temporary checkout passed.
- Restored the default container after the override test.
- Toolchain smoke test in the restored default container passed for .NET,
  Java, Maven, Go, Rust, Python, Node.js, Swift, SourceKit-LSP, OpenCode,
  Codex CLI, Claude Code, Antigravity CLI, GitHub Copilot CLI, Syft, and Spec Kit.
- Generated SBOM without rebuilding:
  `sboms/2026-07-11-localhost-absdd-image-sandbox_ade-latest.cdx.json`.
  The SBOM is CycloneDX 1.7 with 23,937 components and remains an ignored build
  artifact.

## Compliance Plan

- No new `COMPLIANCE-PLAN_RL-SE-001` task was claimed as formally completed.
- The audit stop wrapper was used before Compose teardown and wrote metadata to
  the ignored audit log directory.
- Human-only approvals, branch protection/rulesets, provider/model approvals,
  secret rotation, public repository visibility, and external register entries
  remain unchanged and open for the responsible owner/admin.

## Open / Skipped

- Windows/WSL2 runtime validation: Open.
  Owner: Maintainer.
  Follow-up: run the same default and override checks with the platform's
  active Podman endpoint.
  Re-evaluation trigger: before claiming cross-platform runtime acceptance or
  publishing a release based on this image.
- Linux runtime validation: Open.
  Owner: Maintainer.
  Follow-up: run the same default and override checks with direct local Podman
  or the active local Podman socket.
  Re-evaluation trigger: before claiming cross-platform runtime acceptance or
  publishing a release based on this image.
- Temporary override checkout under
  `/private/tmp/absdd-home-baseline-override.3OUZ5Q/home-baseline` was removed
  after validation.

## Validation

- `git diff --check`: passed.
- `python3 scripts/check-home-baseline-lock.py`: passed.
- `python3 scripts/check-dockerfile-arg-renovate.py`: passed.
- Bash syntax checks for changed scripts and existing audit/smoke/compose-down
  scripts: passed.
- `podman-compose config`: passed.
- `HOME_BASELINE_DIR=/Users/thorstenhindermann/home-baseline-tmp podman-compose
  -f compose.yml -f compose.home-baseline.yml config`: passed.
- Renovate config validation via `npx --yes --package renovate@latest
  renovate-config-validator renovate.json`: passed.
- `uvx pre-commit run --all-files`: passed after this log was added.
