# Agent Session Log - 2026-08-14 09:48 CEST

## Umfang / Scope

- PowerShell Core als zweite Skriptsprache neben Python im Lern-Sandbox-Profil
  verankert. Das gepinnte MCR-.NET-SDK-Basisimage liefert PowerShell; der
  Dockerfile-ARG `POWERSHELL_VERSION` und ein Build-Gate pruefen die erwartete
  Version `7.6.1`, ohne eine zweite PowerShell-Installation einzufuehren.
- Bash- und PowerShell-Dispatcher fuer nicht-interaktive Prompts an Codex,
  Claude Code, OpenCode, GitHub Copilot CLI, Gemini CLI und Antigravity CLI
  erstellt. Die Repo-Skripte adressieren standardmaessig den Compose-Service;
  die Image-Installationen laufen lokal im Container.
- Den persistenten Projektbereich `/powershell-projects`, Renovate-Metadaten,
  CI-/Pre-Commit-Pruefungen sowie bilinguale Lernenden-, Sicherheits- und
  Agent-Guidance-Dokumentation ergaenzt.
- Keine offene ID aus `COMPLIANCE-PLAN_RL-SE-001.md` als erledigt markiert und
  keine formale Sandbox-, Provider-, Rechts- oder Public-Release-Freigabe
  beansprucht.

*Added PowerShell Core as the second scripting language, paired Bash and
PowerShell prompt dispatchers for all six agent CLIs, a persistent PowerShell
project mount, dependency metadata, automated checks, and bilingual guidance.
No compliance-plan item or formal approval is claimed as complete.*

## Pruefungen / Verification

- `git diff --check`, Bash-Syntaxpruefung, JSON-Pruefung, Dockerfile-/Renovate-
  Metadatenpruefung und Home-Baseline-Lock-Pruefung bestanden.
- Bash-/PowerShell-Paritaetstests fuer alle sechs Agent-Mappings, Prompt-
  Redaction, Standard-Input, Prompt-Datei, Standardziel und Fehlerbehandlung
  bestanden.
- Vollstaendige Python-Test-Discovery unter `scripts/tests/` mit Exitcode `0`
  beendet; ein umgebungsabhaengiger bestehender Test wurde uebersprungen.
- PSScriptAnalyzer fuer `scripts/agent-prompt.ps1` ohne Finding beendet.
- `podman-compose config` sowie die Konfiguration mit lokalem
  Home-Baseline-Override bestanden.
- `pre-commit run --all-files` und ein zusaetzlicher Lauf mit den neuen,
  bislang ungetrackten Dispatcher-Dateien bestanden; der Gitleaks-Scan meldete
  keine hart codierten Secrets.
- Nach der Storage-Reparatur den finalen Build mit deaktivierten Buildah-
  Zwischenschichten ausgefuehrt:
  `BUILDAH_LAYERS=false podman compose build --pull=always ade`. Erzeugte
  Image-ID:
  `fe2a96ae850a3414149bbd909492ac92fdacd7a64953ed62a9d21cd2a7ba1bed`.
- Runtime-Smoke-Test fuer .NET, Java, Go, Rust, Python, PowerShell, Node.js,
  Swift, alle Agent-CLIs, Syft und Spec Kit bestanden. PowerShell `7.6.1`, der
  beschreibbare Mount `/powershell-projects` sowie beide Dispatcher-Dry-Runs
  wurden zusaetzlich geprueft.
- `podman system check` war sowohl nach dem finalen Build als auch bei
  laufendem Container erfolgreich. Container `503c75474e87` laeuft aus dem
  finalen Image.

*Static checks, dispatcher parity tests, repository tests, PSScriptAnalyzer,
Compose validation, pre-commit, Gitleaks, the final image build, runtime smoke
tests, and storage integrity checks passed.*

## Podman-Storage-Reparatur / Podman Storage Repair

- Nach ausdruecklicher Freigabe wurde `podman system check --repair` ohne
  `--force` ausgefuehrt. Vor der Reparatur waren keine Container vorhanden;
  die sechs persistenten Projekt- und Agent-Volumes wurden inventarisiert.
- Die Podman-Machine wurde mit dem offiziellen Machine-OS-Image
  `quay.io/podman/machine-os:6.0` von Server `5.8.2` auf `6.0.2` aktualisiert.
  Client und Server verwenden nun beide Podman `6.0.2`.
- Normale Builds mit Buildah-Zwischenschichten reproduzierten danach eine
  Metadatenabweichung an `/etc`. Die betroffenen Images wurden mit
  `podman system check --repair` und `podman image prune -a -f` entfernt; die
  Volumes wurden nicht geloescht.
- Ein erster layerloser Neuaufbau lief beim finalen Image-Commit in einen
  temporaeren Platzmangel. Nach dem Entfernen ausschliesslich ungenutzter
  Images waren 44 GB frei; der wiederholte layerlose Build war erfolgreich.
- Alle sechs Volumes blieben erhalten. Nach finalem Build und Containerstart
  standen 35 GB frei; beide vollstaendigen Storage-Checks endeten mit Exitcode
  `0`.

*After explicit approval, Podman storage was repaired without `--force`, the
machine OS was aligned to Podman 6.0.2, damaged and unused images were removed,
and the image was rebuilt without Buildah intermediate layers. All six volumes
were preserved, and full integrity checks passed before and after container
startup.*

## Plattformabdeckung / Platform Coverage

- Statische Hostpruefung und Build: macOS auf Apple Silicon mit Linux-arm64-
  Podman-Machine.
- Praktische Laufzeitpruefung auf macOS/Apple Silicon bestanden.
- Windows/WSL2 und nativer Linux-Host waren nicht verfuegbar.

*Static host validation, the build, and runtime validation passed on
macOS/Apple Silicon. Windows/WSL2 and native Linux were unavailable.*
