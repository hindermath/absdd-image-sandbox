# Agent Session Log 2026-07-15 09:37

- Completed: updated `home-baseline.lock.json` to the public
  `home-baseline` release `v0.14.0` and exact commit
  `a6a6ced3c2a7c3f4cfe73838167340eb501c1293`.
- Completed: documented the container boundary in `README.md` and all four
  agent-guidance files. The level-0 reference is read directly inside the
  sandbox; writing `sync-home.*` runs targeting `/home/adedev` are blocked and
  belong on the host. Read-only check and preview modes remain available.
- Completed validation: `python3 scripts/check-dockerfile-arg-renovate.py`,
  `python3 scripts/check-home-baseline-lock.py`, default `podman-compose
  config`, the `compose.home-baseline.yml` override with the local level-0
  checkout, `bash scripts/scan-agent-secrets.sh --fail-on-high .`, `git diff
  --check`, and `uvx pre-commit run --all-files` passed.
- No compliance-plan task ID changed status. This maintenance change preserves
  the existing image and mount architecture and updates the pinned level-0
  reference plus its documented execution boundary.
- Additional local runtime check: `podman compose build --pull` was attempted
  although no Dockerfile changed. The first attempt stopped at `apt` because
  the existing Podman VM clock was behind the host clock. Restarting the
  AppleHV VM while the macOS session was locked booted the guest, but the
  graphical `vfkit` process did not remain active. This is a local host/runtime
  limitation, not a repository validation failure; the required static checks
  above passed.
