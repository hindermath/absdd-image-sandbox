# Agentensitzung / Agent Session: Sandbox- und Flotteninstallation

Zeit / time: `2026-09-12T23:18:15+02:00`

## Erledigt / Completed

- Die Installations-Learnings wurden fuer Sandbox, Toolchain-Preflight,
  Codex-Isolation und CI konkret umgesetzt. / The installation lessons were
  implemented for the sandbox, toolchain preflight, Codex isolation, and CI.
- `actionlint` 1.7.12 und das .NET-Kompatibilitaets-SDK 10.0.301 wurden fuer
  `amd64` und `arm64` hashgebunden installiert. / `actionlint` 1.7.12 and the
  .NET 10.0.301 compatibility SDK were installed with architecture-specific
  hashes for `amd64` and `arm64`.
- Ein JSON-Preflight weist Plattform, Architektur, Werkzeugstatus und die
  exakte `global.json`-SDK-Auswahl nach. `gh` bleibt auf der getrennten Control
  Plane. / A JSON preflight proves platform, architecture, tool status, and
  exact `global.json` SDK selection. `gh` remains on the separate control
  plane.
- Codex wurde im rootless Podman-Container mit einem image-eigenen
  Transaktionspfad ausgefuehrt. Schreibzugriff im Arbeitsbereich sowie
  Lese-/Schreibsperren fuer Authentifizierung und Metadaten wurden praktisch
  bestaetigt. / Codex ran in rootless Podman using an image-owned transaction
  path. Workspace writes and authentication/metadata read/write restrictions
  were verified in practice.
- Das finale lokale ARM64-Image `f51262f3189e` wurde ohne Capabilities und ohne
  privilegierten Container gebaut. Der vollstaendige Toolchain-Smoke-Test und
  90 Repositorytests waren gruen; drei nicht anwendbare Plattformfaelle wurden
  uebersprungen. / The final local ARM64 image `f51262f3189e` was built without
  capabilities or a privileged container. The complete toolchain smoke test
  and 90 repository tests passed; three non-applicable platform cases skipped.

## Grenzen / Boundaries

- Direkte Host-Bind-Mounts werden nicht als verschachtelte Codex-
  Schreibwurzel verwendet. Import und Export bleiben kontrollierte
  Transaktionsschritte. / Direct host bind mounts are not used as nested Codex
  writable roots. Import and export remain controlled transaction steps.
- Provider-Credentials, Rulesets, Pull Requests und Merge-Aktionen verbleiben
  auf der Host-Control-Plane. / Provider credentials, rulesets, pull requests,
  and merge operations remain on the host control plane.

## Dokumentationsauswirkung / Documentation Impact

`UpdateRequired`. Kanonische Quellen und Owner sind Dockerfile,
Sandboxkonfiguration und Repository-Maintainer. Betroffen sind Lernende,
Maintainer und Agenten; README, Image-Aufbau, Validierung, Sandbox-Freigabe und
Isolationsnachweis bilden den aktualisierten Leserpfad. Die Sprachpartner stehen
inline, die Distribution ist `RepositoryDocumentation`, und ein Home-Sync ist
nicht erforderlich. Aenderungen an Toolversionen, Mounts, Codex-Sandbox oder
Control-Plane-Grenzen loesen eine Neubewertung aus. / Canonical sources and
owner are the Dockerfile, sandbox configuration, and repository maintainers.
The updated reader path covers README, image build, validation, sandbox
approval, and isolation evidence for learners, maintainers, and agents.
Language partners are inline, distribution is `RepositoryDocumentation`, no
Home sync is required, and changes to tool versions, mounts, Codex sandboxing,
or control-plane boundaries trigger re-evaluation.

## Eskaliert / Escalated

- Keine. / None.
