# Agent Session Log - 2026-08-09 12:49 CEST

## Umfang / Scope

- Home Baseline `v0.17.1` am Commit
  `073c6d26b9e038a2bd32f0eefe7ee83a9a9f8312` in das Sandbox-Image gepinnt.
- Das Image neu gebaut, den laufenden Container ohne Volume-Loeschung neu
  erstellt und die Build-SBOM erneut erzeugt.
- P2-3 als wiederholte Build-Evidenz ausgefuehrt; keine neue formelle
  Compliance- oder Sandbox-Freigabe beansprucht.

*Pinned Home Baseline `v0.17.1`, rebuilt and recreated the sandbox without
deleting volumes, and regenerated the image SBOM. This run repeats P2-3 build
evidence and does not claim a new formal compliance or sandbox approval.*

## Pruefungen / Verification

- Lock-Validator und Compose-Konfiguration ohne Override, mit lokalem
  Home-Baseline-Override sowie ueber `podman compose` bestanden.
- `podman compose build --pull` bestanden; finales Image
  `bfc5469b925a2c309ec6a00202a0b6c6b4b6cd351183dc084874d2771a2ce7d8`.
- Eingebetteter Commit, read-only Source, kanonischer Link, veralteter
  Kompatibilitaetslink und lesbare Codex-Anforderungen bestanden.
- OpenCode-Modellkatalog und CLI-Starts fuer OpenCode, Codex, Claude Code,
  Gemini CLI, Antigravity, Copilot, Syft und Spec Kit bestanden.
- Reale Minimalprogramme fuer .NET, Java, Go, Rust, Python und Swift wurden im
  Container kompiliert beziehungsweise ausgefuehrt.
- Wrapper ohne Modus, Dry-run und Check-only blieben schreibfrei. Der
  vorhandene abweichende v0.17.0-Runtimebestand wurde im Live-Container
  konfliktgeschuetzt abgelehnt; geschuetzte Agentenzustaende und alle fuenf
  Persistenz-Sentinels blieben unveraendert.
- In einem disposable Container bestanden erster Apply, Check-only und zweiter
  inhaltlicher No-op. Nur die beiden Laufmetadatendateien unter
  `~/.home-baseline/` erhielten neue Ergebnis- und Zustandsdaten.
- Container-Neustart, eingebetteter Commit und Volume-Persistenz bestanden.
- CycloneDX 1.7 SBOM mit Syft 1.46.0 und 23.964 Komponenten erzeugt. Das
  ignorierte Artefakt
  `sboms/2026-08-09-localhost-absdd-image-sandbox_ade-latest.cdx.json` ist etwa
  11,5 MB gross.

*Lock, Compose, image build, source integrity, agent CLIs, all six language
programs, conflict protection, clean-home apply/idempotency, restart,
persistence, and the CycloneDX SBOM passed. Existing live runtime differences
were not overwritten.*

## Plattformabdeckung / Platform Coverage

- Praktische Host- und Podman-Lebenszykluspruefung: macOS auf Apple Silicon mit
  Linux-arm64-Podman-Machine.
- Windows/WSL2 und ein nativer Linux-Host waren nicht verfuegbar.

*Practical lifecycle coverage was macOS on Apple Silicon with a Linux-arm64
Podman machine. Windows/WSL2 and a native Linux host were unavailable.*
